trapz method and gsl integration (now commented) give the same results (same coupling matrix, same eigenvalues, up to the 4th digit at least)
But trapz method is about 6 times faster (for the same precision in impedancesum)...
